---
title: "PLACEHOLDER — Replace With Your Real Blog Posts"
description: "This is a sample post so the blog section renders correctly. Delete it and add your existing articles as markdown files in this folder."
pubDate: 2026-01-05
draft: false
---

This is placeholder content. You mentioned you already have blog posts — once you send them over (or paste them in), add each one as a new `.md` file in `src/content/blog/`, following this same frontmatter format (`title`, `description`, `pubDate`).

Each file automatically becomes a page at `/blog/<file-name>/` and shows up on the `/blog/` index, newest first.
